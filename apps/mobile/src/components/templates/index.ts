/**
 * Templates — page-level layout only. They place regions and own no data
 * fetching, so a page can swap its content without touching the structure.
 */
export { AppDetailTemplate } from './AppDetailTemplate';
export { AuthTemplate } from './AuthTemplate';
export { CenteredTemplate } from './CenteredTemplate';
export { ListTemplate } from './ListTemplate';
export { ScrollTemplate } from './ScrollTemplate';
