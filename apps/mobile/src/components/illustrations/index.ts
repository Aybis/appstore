/**
 * Onboarding artwork, generated from unDraw by scripts/undraw.mjs.
 *
 * Each one draws into whatever box it is given and letterboxes itself inside
 * it — react-native-svg's default `preserveAspectRatio` centres and fits. That
 * is why the three sit in an identically-sized box on the carousel despite
 * having quite different intrinsic ratios: the copy underneath keeps a fixed
 * baseline as you page, instead of jumping when a wider drawing arrives.
 */

export { Catalog } from './Catalog';
export { Install } from './Install';
export { Updates } from './Updates';
export { useIllustrationPalette, type IllustrationProps } from './palette';
